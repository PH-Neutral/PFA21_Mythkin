using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class InputSystem {
    /*
    public struct Event {
        public Event() {

        }
    }
    //public static KeyCode keyForward = KeyCode.Z, keyBackward = KeyCode.S, keyRight = KeyCode.D, keyLeft = KeyCode.Q; 
    public static KeyCode keyJump = KeyCode.Space, keySprint = KeyCode.LeftShift;
    public static KeyCode keyInteract = KeyCode.E;

    public static Vector3 Movement {
        get { return new Vector3(Input.GetAxis("Horizontal"), IsJumping ? 1 : 0, Input.GetAxis("Vertical")); }
    }
    public static bool IsSprinting {
        get { return Input.GetKey(keySprint); }
    }
    public static bool IsJumping {
        get { return Input.GetKey(keyJump); }
    }
    public static bool IsInteracting {
        get { return Input.GetKey(keyJump); }
    }
    public static bool IsAiming {
        get { return Input.GetKey(keyJump); }
    }
    public static bool IsThrowing {
        get { return Input.GetKey(keyJump); }
    }
    */
}